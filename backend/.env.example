# Kết nối PostGIS. Trong docker-compose đã set sẵn (db:5432).
# Chạy local (ngoài docker) thì trỏ tới cổng 5433 đã map ra.
DATABASE_URL=postgresql://ranhthua:ranhthua@localhost:5433/ranhthua

# (tuỳ chọn) override khi import
# SHP_PATH=_shp_seed/TruongLinh_Chialo.shp
# LAYER_ID=truonglinh-chialo
